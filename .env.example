# Polling interval in seconds
POLL_SECONDS=180

# Webhooks configuration (JSON format)
# Each webhook has:
#   - url: The Pushcut webhook URL
#   - type: "homescreen" (full widget with arrow, delta, colors) or "lockscreen" (price only)
#   - name: Optional friendly name for logging
#
# Homescreen inputs: price, arrow icon, delta display, color1, color2
# Lockscreen inputs: price only (input0)

WEBHOOKS=[{"url": "https://api.pushcut.io/YOUR_KEY/widgets/eDahab-Home", "type": "homescreen", "name": "iPhone Home"}, {"url": "https://api.pushcut.io/YOUR_KEY/widgets/eDahab-Lock", "type": "lockscreen", "name": "iPhone Lock"}]
